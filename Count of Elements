#include <iostream>
#include<bits/stdc++.h>
using namespace std;
int main() {
    vector<int> v;
    unordered_map<int,int> m;
    int n,arr[n],k;
    cin>>n;
    for(int i=0;i<n;i++){
        cin>>arr[i];
    }
    for(int i=0;i<n;i++){
        m[arr[i]]++;
    }
    cin>>k;
    for(auto itr:m){
        v.push_back(itr.second);
    }
    for(int i=0;i<v.size();i++){
        cout<<v[i]<<" ";
        }
    return 0;
}
